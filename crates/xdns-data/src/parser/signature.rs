use shared::common::Result;
use crate::models::signature::Signature;

use crate::prelude::Parser;

impl Parser for Signature {
    fn parse(input: &str) -> Result<Self> {
        // let signature = input.split_whitespace().last().unwrap();
        todo!()
    }
}
